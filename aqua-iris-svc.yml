apiVersion: v1
kind: Service
metadata:
  labels:
    app: aqua-iris
  name: aqua-iris
  namespace: aqua
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: aqua-iris
  type: ClusterIP
